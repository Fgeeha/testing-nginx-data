services:
  nginx-browser-test:
    build: .
    ports:
      - "8080:80"
    volumes:
      - ./logs:/var/log/nginx
      - ./html:/usr/share/nginx/html
    environment:
      - NGINX_HOST=localhost
      - NGINX_PORT=80
    restart: unless-stopped

volumes:
  logs: